const nombre = "Santiago"
const apellido = "Yara"

const persona = {
    nombre: nombre,
    apellido: apellido
}

//sessionStorage.setItem("persona",JSON.stringify(persona))

//localStorage.setItem("persona",JSON.stringify(persona))
let min = new Date()
//document.cookie = `persona = ${JSON.stringify(persona)}; expires =` + new Date(min.setMinutes(min.getMinutes()+2)).toUTCString()

